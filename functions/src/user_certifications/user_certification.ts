interface UserCertification {
    userId: string,
    certificationId: string,
    isCompleted: string,
    startDate: string,
    completionDate: string,
    expiryDate: string,
    rating: string
}
