interface UserCertification {
    userId: string,
    certificationId: string,
    isCompleted: boolean,
    startDate: string,
    completionDate: string,
    expiryDate: string,
    rating: number
}
